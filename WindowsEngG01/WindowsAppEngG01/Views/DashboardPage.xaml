<Page
    x:Class="WindowsAppEngG01.Views.DashboardPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:WindowsAppEngG01.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
    <Hub Header="Dashboard" SectionHeaderClick="Hub_SectionHeaderClick">
        <HubSection Width="300" Header="Companies">
            <DataTemplate>
                <StackPanel>
                    <GridView x:Name="lvCompanies" ItemsSource="{Binding Companies}" SelectionChanged="LvCompanies_SelectionChanged">
                        <GridView.ItemTemplate>
                            <DataTemplate>
                                <StackPanel Height="100" Width="100" Orientation="Vertical" Background="CadetBlue">
                                    <Image Source="{Binding Logo}" Height="70" Width="70" VerticalAlignment="Center" Margin="5" Stretch="UniformToFill" />
                                    <TextBlock Text="{Binding Name}" HorizontalAlignment="Stretch" TextAlignment="Center" TextWrapping="Wrap" />
                                </StackPanel>
                            </DataTemplate>
                        </GridView.ItemTemplate>
                        <GridView.ItemsPanel>
                            <ItemsPanelTemplate>
                                <ItemsWrapGrid MaximumRowsOrColumns="4" Orientation="Horizontal" />
                            </ItemsPanelTemplate>
                        </GridView.ItemsPanel>
                    </GridView>
                    <StackPanel x:Name="stpAddCompany" Height="100" Width="100" Orientation="Vertical" Background="CadetBlue" Tapped="StpAddCompany_Tapped">
                        <TextBlock Text="Add Company" TextWrapping="Wrap" TextAlignment="Center" VerticalAlignment="Stretch" HorizontalAlignment="Stretch" />
                        <Image />
                    </StackPanel>
                </StackPanel>
            </DataTemplate>
        </HubSection>

        <HubSection x:Name="Tech" Header="Details" IsHeaderInteractive="True"  
        Background="#F4F4F4" MinWidth="600">
            <DataTemplate>
                <Grid>
                    
                </Grid>
            </DataTemplate>
        </HubSection>

        <HubSection x:Name="Sports" Header="Promotions" IsHeaderInteractive="True" 
        Background="#F9F9F9" MinWidth="300">
            <DataTemplate>
                <Grid>
                    
                </Grid>
            </DataTemplate>
        </HubSection>
    </Hub>
</Page>
