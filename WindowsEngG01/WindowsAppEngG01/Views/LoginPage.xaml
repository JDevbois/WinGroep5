<Page
    x:Class="WindowsAppEngG01.Views.LoginPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:WindowsAppEngG01.Views"
    xmlns:util="using:WindowsAppEngG01.Utils"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
    <Page.Resources>
        <util:BooleanToVisibilityConverter x:Key="booleanToVisibilityConverter"/>
    </Page.Resources>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <TextBlock Text="E-mail:" Grid.Row="0" Grid.Column="0" />
        <TextBlock Text="Password:" Grid.Row="1" Grid.Column="0" />
        <TextBox Text="{Binding Email}" Grid.Row="0" Grid.Column="1" />
        <PasswordBox x:Name="passwordBox" Grid.Row="1" Grid.Column="1" />
        <StackPanel Orientation="Horizontal" Grid.Row="2" Grid.Column="1">
            
            <Button Content="Log in" Command="{Binding LoginCommand, Mode=OneTime}"
                    x:Name="btnLogIn"
                    CommandParameter="{Binding ElementName=passwordBox}"
                    Visibility="{Binding CanLogin, Converter={StaticResource booleanToVisibilityConverter}, Mode=TwoWay}"
                    HorizontalAlignment="Center"/>
            
            <Button Content="Log out" 
                    x:Name="btnLogOut"
                    Command="{Binding LogoutCommand, Mode=OneTime}"
                    Visibility="{Binding CanLogout, Converter={StaticResource booleanToVisibilityConverter}, Mode=TwoWay}"
                    HorizontalAlignment="Center" Margin="2,0,0,0"/>
        </StackPanel>
        <TextBlock Text="Don't have an account yet? Click here to register." Grid.Row="3" Grid.Column="1" x:Name="txtRegister" Tapped="TxtRegister_Tapped" />
    </Grid>
</Page>
